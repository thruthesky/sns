---
name: snsweb-deploy-overview
version: 1.0.0
description: SNS 웹 배포 가이드 문서의 SED 사양
author: JaeHo Song
email: thruthesky@gmail.com
homepage: https://github.com/thruthesky/
funding: ""
license: GPL
dependencies: []
"프로젝트 이름": snsweb
---

## Overview
- 이 문서는 "SNS 웹 배포 가이드"에 대한 세부 사양을 정리하며, 기존 내용을 그대로 유지한 채 SED 구조에 맞춰 제공합니다.

## Requirements
- 문서 전반에 걸쳐 소개되는 지침과 참고 사항을 모두 숙지해야 하며, 별도의 추가 선행 조건은 원문 각 절에서 제시되는 내용을 따릅니다.

## Workflow
1. 아래 `## Detail Items` 절에 포함된 원문 목차를 검토합니다.
2. 필요한 경우 원문의 각 절을 순서대로 읽으며 프로젝트 작업 흐름에 반영합니다.
3. 문서에 명시된 모든 지침을 확인한 뒤 실제 개발 단계에 적용합니다.

## Detail Items
- 이하에는 기존 문서의 모든 내용을 원형 그대로 포함하여 참조할 수 있도록 구성했습니다.

# SNS 웹 배포 가이드

> 본 문서는 SNS 웹 프로젝트를 프로덕션 환경에 배포하는 방법을 안내합니다.

## 📋 목차

- [배포 전 준비사항](#배포-전-준비사항)
- [프로젝트 빌드](#프로젝트-빌드)
- [GitHub Pages 배포](#github-pages-배포)
- [문제 해결](#문제-해결)

---

## 배포 전 준비사항

배포하기 전에 다음 사항을 확인하세요:

### ✅ 체크리스트

- [ ] [웹 개발 퀵 스타트](sns-web-quick-start.md) 문서를 참고하여 개발 환경이 올바르게 설정되어 있는지 확인
- [ ] 모든 필수 의존성 패키지가 설치되어 있는지 확인
- [ ] Firebase 설정이 올바르게 구성되어 있는지 확인 (`.env` 파일)
- [ ] 로컬 개발 서버에서 정상 작동하는지 테스트

---

## 프로젝트 빌드

### 1. 빌드 명령어 실행

프로젝트 루트의 `web` 디렉토리에서 빌드를 실행합니다:

```bash
cd web
npm run build
```

### 2. 빌드 프로세스 이해하기

`npm run build`는 두 단계로 진행됩니다:

#### 2-1. 라이브러리 빌드 (`build:lib`)

**사용하는 설정 파일**: `vite.config.js`

**빌드 대상**: `src/lib/index.js` (라이브러리 진입점)

**생성되는 파일**:
- `dist/sns-components.es.js` - ES 모듈 형식 번들
- `dist/sns-components.umd.js` - UMD 형식 번들 (브라우저 호환)

**프로세스**:
1. `src/lib/index.js`에서 모든 Custom Elements 컴포넌트를 import
2. `src/lib/components/*.wc.svelte` 파일들을 Web Components로 컴파일
3. Firebase는 외부 의존성으로 처리 (번들에 포함하지 않음)
4. ES 모듈과 UMD 두 가지 형식으로 번들링

#### 2-2. 데모 앱 빌드 (`build:demo`)

**사용하는 설정 파일**: `vite.demo.config.js`

**빌드 대상**: `src/demo/index.html` (데모 앱 HTML)

**생성되는 파일**:
- `dist/index.html` - 데모 앱의 HTML 파일
- `dist/assets/*.js` - 번들링된 JavaScript 파일
- `dist/assets/*.css` - 번들링된 CSS 파일

**프로세스**:
1. `src/demo/index.html`을 진입점으로 사용
2. HTML에서 참조하는 `src/demo/main.js` 로드
3. `main.js`에서 `App.svelte` 컴포넌트를 마운트
4. 모든 의존성을 번들링하여 `dist/assets/` 폴더에 저장
5. `index.html`의 스크립트 경로를 번들 파일로 자동 업데이트

**빌드 흐름도**:
```
src/demo/index.html
    ↓ (참조)
src/demo/main.js
    ↓ (import)
src/demo/App.svelte
    ↓ (import)
src/lib/index.js (Custom Elements)
    ↓ (빌드)
dist/index.html + dist/assets/[hash].js
```

### 3. 빌드 결과 확인

빌드가 완료되면 다음과 같은 출력을 볼 수 있습니다:

```bash
# 라이브러리 빌드
✓ built in [시간]ms
dist/sns-components.es.js  [크기]
dist/sns-components.umd.js [크기]

# 데모 앱 빌드
✓ built in [시간]ms
dist/index.html                     [크기]
dist/assets/index-[hash].js         [크기]
dist/assets/index-[hash].css        [크기]
```

최종 빌드 결과물 구조:

```
web/dist/
├── sns-components.es.js      # 라이브러리: ES 모듈 번들
├── sns-components.umd.js     # 라이브러리: UMD 번들
├── index.html                # 데모 앱: HTML 진입점
├── vite.svg                  # 데모 앱: 아이콘
└── assets/                   # 데모 앱: 번들링된 리소스
    ├── index-[hash].js       #   - JavaScript 번들
    └── index-[hash].css      #   - CSS 번들
```

### 4. 개별 빌드 명령어

필요시 라이브러리나 데모 앱을 개별적으로 빌드할 수 있습니다:

```bash
# 라이브러리만 빌드
npm run build:lib

# 데모 앱만 빌드
npm run build:demo
```

### 5. 빌드 검증

빌드 결과를 로컬에서 확인하는 방법은 두 가지가 있습니다:

#### 방법 1: Vite Preview 사용 (권장)

Vite의 내장 프리뷰 서버를 사용합니다:

```bash
npm run preview
```

브라우저에서 `http://localhost:4173` (또는 표시된 포트)로 접속하여 확인합니다.

**주의**: `preview`는 `dist/index.html`을 서빙하므로, 빌드를 먼저 실행해야 합니다.

#### 방법 2: http-server 사용

더 간단한 정적 파일 서버를 원한다면 `http-server`를 사용할 수 있습니다:

**1단계: http-server 설치 (최초 1회)**

```bash
# 전역 설치 (관리자 권한 필요)
sudo npm install -g http-server

# 또는 npx로 설치 없이 실행 가능 (아래 2단계 참고)
```

**2단계: 빌드 결과물 서빙**

```bash
# 설치한 경우
cd web
http-server dist

# 또는 설치 없이 npx로 실행
npx http-server dist
```

**3단계: 브라우저에서 확인**

터미널에 표시된 URL로 접속합니다 (일반적으로 `http://127.0.0.1:8080`):

```
Starting up http-server, serving dist

Available on:
  http://127.0.0.1:8080
  http://192.168.x.x:8080
Hit CTRL-C to stop the server
```

**http-server 유용한 옵션**:

```bash
# 특정 포트로 실행
http-server dist -p 3000

# 자동으로 브라우저 열기
http-server dist -o

# CORS 활성화 (API 테스트 시 유용)
http-server dist --cors

# 캐시 비활성화 (개발 중 유용)
http-server dist -c-1
```

**비교**:

| 항목 | Vite Preview | http-server |
|------|--------------|-------------|
| 설치 | 프로젝트에 포함 | 별도 설치 필요 |
| 속도 | 빠름 | 빠름 |
| 설정 | package.json에 정의됨 | 명령줄 옵션 |
| 권장 용도 | 프로젝트 개발 중 | 간단한 정적 파일 서빙 |

---

## GitHub Pages 배포

GitHub Pages를 사용하여 무료로 호스팅할 수 있습니다.

### 방법 1: 수동 배포

#### 1단계: dist 폴더 준비

빌드가 완료된 `dist/` 폴더의 내용을 준비합니다.

#### 2단계: gh-pages 브랜치 생성

```bash
# gh-pages 브랜치가 없다면 생성
git checkout --orphan gh-pages
git rm -rf .
```

#### 3단계: 빌드 파일 복사 및 커밋

```bash
# dist 폴더의 내용을 복사
cp -r web/dist/* .

# 커밋 및 푸시
git add .
git commit -m "Deploy to GitHub Pages"
git push origin gh-pages
```

#### 4단계: GitHub Pages 설정

1. GitHub 저장소로 이동
2. **Settings** > **Pages** 섹션으로 이동
3. **Source** 섹션에서:
   - **Branch**: `gh-pages` 선택
   - **Folder**: `/ (root)` 선택
4. **Save** 버튼 클릭

#### 5단계: 배포 확인

- 몇 분 후 사이트가 `https://[username].github.io/[repository-name]/`에 배포됩니다
- **Actions** 탭에서 배포 진행 상황을 확인할 수 있습니다

### 방법 2: gh-pages 패키지 사용

더 편리한 자동 배포를 위해 `gh-pages` 패키지를 사용할 수 있습니다.

#### 1단계: gh-pages 패키지 설치

```bash
cd web
npm install --save-dev gh-pages
```

#### 2단계: package.json에 배포 스크립트 추가

`web/package.json` 파일에 다음 스크립트를 추가합니다:

```json
{
  "scripts": {
    "build": "vite build",
    "deploy": "npm run build && gh-pages -d dist"
  }
}
```

#### 3단계: 배포 실행

```bash
npm run deploy
```

이 명령어는 자동으로:
1. 프로젝트를 빌드하고
2. `dist/` 폴더의 내용을 `gh-pages` 브랜치에 푸시합니다

### 방법 3: GitHub Actions 자동 배포 (권장) ⭐

GitHub Actions를 사용하면 `main` 브랜치에 코드를 push할 때마다 자동으로 빌드하고 배포합니다.

#### 1단계: 워크플로우 파일 확인

프로젝트에는 이미 `.github/workflows/gh-pages.yaml` 파일이 설정되어 있습니다:

```yaml
name: Deploy to GitHub Pages
on:
  push:
    branches: [main]
permissions:
  contents: write
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: cd web && npm ci && npm run build
      - name: Deploy
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./web/dist
```

**워크플로우 동작 과정**:

1. **트리거**: `main` 브랜치에 push 이벤트 발생
2. **체크아웃**: 저장소 코드를 가져옴
3. **Node.js 설정**: Node.js 20 버전 설치
4. **빌드**: `web` 폴더로 이동하여 의존성 설치 및 빌드 실행
   - `npm ci`: package-lock.json을 기준으로 의존성 클린 설치
   - `npm run build`: 라이브러리와 데모 앱 모두 빌드
5. **배포**: `web/dist` 폴더 내용을 `gh-pages` 브랜치에 푸시

#### 2단계: GitHub Pages 설정

**최초 1회만 설정**하면 됩니다:

1. GitHub 저장소 페이지로 이동
2. **Settings** 탭 클릭
3. 왼쪽 메뉴에서 **Pages** 클릭
4. **Source** 섹션에서:
   - **Branch**: `gh-pages` 선택
   - **Folder**: `/ (root)` 선택
5. **Save** 버튼 클릭

#### 3단계: 자동 배포 사용하기

이제 코드를 수정하고 `main` 브랜치에 push하면 자동으로 배포됩니다:

```bash
# 코드 수정 후
git add .
git commit -m "Update components"
git push origin main
```

#### 4단계: 배포 상태 확인

1. GitHub 저장소의 **Actions** 탭으로 이동
2. "Deploy to GitHub Pages" 워크플로우의 실행 상태 확인
3. 모든 단계가 성공하면 ✅ 녹색 체크 표시
4. 실패하면 ❌ 빨간색 X 표시 (로그를 클릭하여 에러 확인)

**배포 소요 시간**: 일반적으로 2-3분 정도 소요됩니다.

#### 5단계: 배포된 사이트 확인

배포가 완료되면 다음 URL에서 확인할 수 있습니다:

```
https://[사용자명].github.io/[저장소명]/
```

예: `https://withcenter.github.io/sns/`

#### 자동 배포 장점

✅ **편리함**: 코드 push만 하면 자동으로 배포
✅ **일관성**: 항상 동일한 환경에서 빌드
✅ **추적성**: Actions 탭에서 배포 이력 확인 가능
✅ **안전성**: 빌드 실패 시 자동으로 배포 중단

#### 주의사항

⚠️ **환경 변수 관리**

`.env` 파일은 Git에 커밋되지 않으므로, 프로덕션 환경에서는 다음 중 하나의 방법을 사용하세요:

**방법 1: GitHub Secrets 사용**

1. GitHub 저장소의 **Settings** > **Secrets and variables** > **Actions** 로 이동
2. **New repository secret** 클릭
3. 각 환경 변수를 추가 (예: `VITE_FIREBASE_API_KEY`)
4. 워크플로우 파일에서 환경 변수로 주입:

```yaml
- run: cd web && npm ci && npm run build
  env:
    VITE_FIREBASE_API_KEY: ${{ secrets.VITE_FIREBASE_API_KEY }}
    VITE_FIREBASE_AUTH_DOMAIN: ${{ secrets.VITE_FIREBASE_AUTH_DOMAIN }}
    # 나머지 환경 변수들...
```

**방법 2: 공개 설정값 사용**

프로덕션 전용 Firebase 프로젝트를 생성하고, 해당 설정값을 코드에 직접 포함:

```javascript
// src/lib/utils/firebase.js
const firebaseConfig = {
  apiKey: import.meta.env.VITE_FIREBASE_API_KEY || "프로덕션_API_KEY",
  authDomain: import.meta.env.VITE_FIREBASE_AUTH_DOMAIN || "프로덕션_AUTH_DOMAIN",
  // ...
};
```

⚠️ **빌드 실패 디버깅**

Actions 탭에서 실패한 워크플로우를 클릭하면 상세 로그를 볼 수 있습니다:

1. 실패한 워크플로우 클릭
2. 실패한 job 클릭
3. 실패한 step을 펼쳐서 에러 메시지 확인

---

## 문제 해결

### 빌드 실패

**증상**: `npm run build` 실행 시 에러 발생

**해결 방법**:
1. `node_modules` 삭제 후 재설치:
   ```bash
   rm -rf node_modules package-lock.json
   npm install
   ```
2. Node.js 버전 확인 (Node 18 이상 권장)
3. 에러 메시지를 확인하고 누락된 의존성 설치

### 404 에러

**증상**: 배포된 사이트에서 404 에러 발생

**해결 방법**:
1. GitHub Pages 설정에서 올바른 브랜치(`gh-pages`)와 폴더(`/ root`)가 선택되었는지 확인
2. `index.html` 파일이 `dist/` 폴더의 루트에 있는지 확인
3. 몇 분 후 다시 시도 (GitHub Pages는 배포에 시간이 걸릴 수 있음)

### 환경 변수 문제

**증상**: 배포된 사이트에서 Firebase 연결 실패

**해결 방법**:
1. `.env` 파일이 빌드에 포함되지 않음 (보안상 올바름)
2. 프로덕션 환경에서는 다른 방법으로 환경 변수 관리 필요:
   - GitHub Secrets 사용
   - 빌드 시 환경 변수 주입
   - 별도의 설정 파일 사용

### Custom Domain 설정

**증상**: 커스텀 도메인을 사용하고 싶음

**해결 방법**:
1. `dist/` 폴더에 `CNAME` 파일 추가:
   ```bash
   echo "yourdomain.com" > dist/CNAME
   ```
2. GitHub Pages 설정에서 Custom domain 입력
3. DNS 설정에서 CNAME 레코드 추가

---

## 추가 배포 옵션

### Vercel 배포

1. [Vercel](https://vercel.com) 계정 생성
2. GitHub 저장소 연결
3. 빌드 설정:
   - **Framework Preset**: Vite
   - **Root Directory**: `web`
   - **Build Command**: `npm run build`
   - **Output Directory**: `dist`
4. 배포 완료

### Netlify 배포

1. [Netlify](https://netlify.com) 계정 생성
2. "New site from Git" 선택
3. GitHub 저장소 선택
4. 빌드 설정:
   - **Base directory**: `web`
   - **Build command**: `npm run build`
   - **Publish directory**: `web/dist`
5. 배포 완료

---

## 참고 자료

- [웹 개발 퀵 스타트](sns-web-quick-start.md) - 개발 환경 설정
- [SNS 웹 개발 가이드](sns-web.md) - 웹 개발 전체 가이드
- [Custom Elements 개발 가이드](sns-custom-elements.md) - 컴포넌트 개발 상세 가이드


